#> arcane_core.api:module/effect/if_should_clear/persistent
#
# Call to check if the persistent effects should be cleared during the clear call.
#
# @api

return run execute if entity @s[tag=arcane_core.internal.effect.clear.persistent]
